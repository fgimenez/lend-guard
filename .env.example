# Required
WDK_SEED="your twelve word bip39 seed phrase here"
ANTHROPIC_API_KEY=sk-ant-...

# Optional — strategy (these are the defaults)
CHAINS=ethereum,arbitrum,base,optimism
MIN_HEALTH_FACTOR=1.5
TARGET_HEALTH_FACTOR=2.0
INTERVAL_MINUTES=30
AUDIT_LOG=audit.jsonl

# Optional — custom RPC endpoints (public endpoints used if omitted)
RPC_ETHEREUM=
RPC_ARBITRUM=
RPC_BASE=
RPC_OPTIMISM=

# E2E tests only
RPC_SEPOLIA=
RPC_BASE_SEPOLIA=
